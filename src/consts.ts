const fortniteClassId = 8032;

const interestingFeatures = [
  'game_info',
  'match_info'
];

const windowNames = {
  inGame: 'in_game',
  desktop: 'desktop',
  timer: 'timer'
};

const hotkeys = {
  hotkey1: 'hotkey1'
};

export {
  fortniteClassId,
  interestingFeatures,
  windowNames,
  hotkeys
}